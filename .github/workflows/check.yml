# kaguya.bat check のうち、実行環境を用意しやすい部分だけをPRごとに回す。
# Cargo build（Tauri）はRustツールチェーンとWindows SDKが要るため対象外。
# 実機確認は引き続き kaguya.bat check で行う。
name: check

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    # 本番はWindowsだが、Linuxの方が速く安い。Windows固有の問題（使用中ファイルを
    # 削除できない等）はLinuxでは再現しないため、両方で回す。
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: backend/requirements.txt
      - name: install
        working-directory: backend
        run: python -m pip install -r requirements.txt
      - name: unittest
        working-directory: backend
        run: python -B -m unittest discover -s tests

  frontend:
    # TypeScriptとNode上のテストだけなので、OSに依存しない。
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: install
        working-directory: frontend
        run: npm ci
      - name: typecheck
        working-directory: frontend
        run: npx tsc --noEmit
      - name: tests
        working-directory: frontend
        run: node --test tests/*.test.cjs
      - name: build
        working-directory: frontend
        run: npm run build
