# Copy to .env locally. Never commit or share the filled file.
GEMINI_API_KEY=
# Choose a model after checking availability and the free tier in AI Studio.
GEMINI_MODEL=
# Use a dedicated PostgreSQL database and application role.
DATABASE_URL=postgresql://kaguya:REPLACE_LOCALLY@127.0.0.1:5432/kaguya_ai
